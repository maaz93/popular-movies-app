<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Preference to set the sorting order-->
    <ListPreference
        android:defaultValue="@string/pref_sorting_default"
        android:entries="@array/pref_sort_keys"
        android:entryValues="@array/pref_sort_values"
        android:key="@string/pref_sorting_key"
        android:title="@string/pref_sorting_title" />

</PreferenceScreen>
